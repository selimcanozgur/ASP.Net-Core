@model Student
<p>
    Do you want to delete the student named <b>@Model.FirstName @Model.LastName</b> ?
<form asp-action="Delete" method="post">
    <input type="hidden" value="@Model.StudentId" name="id">
    <button type="submit" class="btn btn-danger">Yes</button>
    <a asp-action="List" class="btn btn-primary">Hayır</a>
</form>
</p>